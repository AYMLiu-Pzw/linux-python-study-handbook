# Linux 指令练习册大纲（基础篇 + 进阶篇）

> 用途：发给制作资料书的老师，作为练习册的设计规格书。老师按本文档的表格逐格填充即可成书。
> 与教科书的关系：教科书 = 边学边动手；本练习册 = **学完教科书后**的独立巩固体系（共约 22~30 小时）。

---

## 一、练习册定位与使用说明（写进书的前言）

1. **定位**：真正学懂 Linux 指令，而不是背指令。每道题背后都有一个"要弄懂的原理"，答案不直接给命令，而是先给思路。
2. **学习顺序**：先学完整本教科书 → 再按顺序做完基础篇 5 阶段 → 再做进阶篇 5 场景。
3. **环境**：以电脑 Ubuntu 为准设计完整题目；标注 🌐 的题需要电脑 Ubuntu（平板 Termux 做不了），其余平板也能做。
4. **题目语言规则**：中文出题，命令名保留英文，并在题目中首次出现时用括号标注含义（如 `df -h`（查看磁盘占用）），**但不标注涉及解题思路的含义**。
5. **安全红线**：进阶篇所有故障都用"沙盒式故障注入"复刻（见第三节），不会伤到真实系统；所有高危命令在搭建手册中标注警告。

---

## 二、给老师的设计规格

### 题目格式（每道题按此表填写）

| 字段 | 要求 |
|---|---|
| 编号 | 如 基础3-2（基础篇阶段3第2题） |
| 题型 | 动手 / 观察 / 组合 / 辨析 |
| 考点 | 一句话说明练什么 |
| 题目指令 | **只给目标，不给过程**。中文描述，命令名保留英文并首次标注含义 |
| 环境标注 | 需要电脑 Ubuntu 的标 🌐 |

### 答案格式（书末集中，按阶段分节；每题必须四步式）

1. **先想什么**：这道题在考什么、从哪里入手（思考过程，不写命令）
2. **为什么用这个命令**：候选命令对比，为什么选它
3. **完整步骤**：可复制执行的命令序列
4. **怎么验证做对了**：预期的输出长什么样、看到什么就算对

### 交稿验收清单

- [ ] 基础篇每个核心概念是否都有 2 个不同场景的变式题？
- [ ] 进阶篇题目是否只给目标、过程全部在答案页？
- [ ] 每道题答案是否都有四步式思考过程？
- [ ] 所有题目是否实际运行验证过（预期输出不是编的）？
- [ ] 危险命令是否有警告标注？
- [ ] 每阶段是否都写明了"最终产出"？

---

## 三、场景复刻方案（进阶篇核心设计，写入书前）

进阶篇每个场景配一份「场景搭建手册」，用**沙盒式故障注入**在读者自己的电脑上安全复刻故障现场。逐场景手段：

| 场景 | 复刻手段 | 安全原理 | 环境 |
|---|---|---|---|
| 1 日志排查 | 练习册提供 30 行假服务脚本（会写日志），故意改坏配置让它启动失败 | 失败只发生在脚本自己的目录里 | 两边均可 |
| 2 磁盘满 | `dd` 造 200MB 镜像文件 → `mkfs` 格式化 → 挂载成独立分区 → 塞满；玩完卸载即消失 | 故障发生在镜像文件里，真磁盘不受影响 | 🌐 电脑 |
| 3 网络排查 | 用 `localhost` + 端口模拟远程服务器：服务没起 / 端口被占 / 防火墙拦截 | 全部在本机网络栈内发生 | 两边均可 |
| 4 服务与进程 | 假服务脚本 + 后台运行 + 故意挂掉；GPU 题分轨（见下） | 进程操作只影响练习用进程 | 两边均可 |
| 5 脚本收尾 | 把前四场景最常用操作写成脚本，运行验证 | 无故障注入，纯写作练习 | 两边均可 |

**GPU 题分轨设计**：有 NVIDIA 显卡的读者用 `nvidia-smi`（查看显卡状态）+ `fuser -v` 定位占用进程做真实题；无 GPU 读者做**等价替代题**——用"普通文件被进程占用"练同一个 `fuser`/`lsof` 定位技能，并在题旁说明"有 GPU 后命令完全一样"。

---

## 四、基础篇大纲（5 阶段，预计 12~16 小时）

> 每个核心概念配 2 个不同场景的变式题（题号后标 ★ 的是同一概念的第二变式）。

### 阶段 1｜文件系统操作：导航、创建、复制、移动、删除

**阶段目标**：理解"目录树"这个核心概念，熟练增删改查，弄懂 `cp` 与 `mv` 的本质区别、删除权限的真正来源。
**对应教科书章节**：命令组 0~3。

| 编号 | 题型 | 考点 | 题目指令（只给目标） |
|---|---|---|---|
| 1-1 | 动手 | 多级目录 | 在你的家目录下建 lab1，在里面一次性创建 a/b/c 三级目录，并弄懂 `mkdir -p`（创建目录）起什么作用 |
| 1-2 ★ | 动手 | 绝对/相对路径 | 换到 /tmp（临时目录）再做一遍建目录练习，这次从别的目录出发，体会绝对路径与相对路径的差别 |
| 1-3 | 动手 | 批量创建 | 一条命令创建 file1.txt ~ file5.txt 五个文件（提示：花括号展开） |
| 1-4 | 观察 | cp 递归 | 试一下不用 `-r` 复制目录会发生什么，把报错记下来并解释它 |
| 1-5 ★ | 动手 | cp 两种语义 | 把文件复制到"已存在的目录"和复制到"一个新名字"，观察结果差别，总结 `cp` 的两条规则 |
| 1-6 | 动手 | mv 双功能 | 用 `mv` 完成一次"搬家 + 改名"二合一操作，说明它和"cp + 删原文件"有什么不同 |
| 1-7 | 辨析 | 删除权限原理 | 造一个"只读文件"，试试能不能删掉它；解释为什么删除文件靠的是**目录**的写权限而不是文件的写权限 |
| 1-8 ★ | 动手 | 安全删除 | 建一棵 3 层目录树，用 `rm -r` 一次删掉；再用 `rm -i`（删除前确认）体验交互模式，养成危险操作确认习惯 |

**最终产出**：独立搭建一棵自己的目录树 + 完成一次完整的"文件搬家"，并用文字总结 `cp`/`mv`/`rm` 三条命令各自的边界。

### 阶段 2｜查看与管道：cat/less/head/tail/grep/重定向

**阶段目标**：弄懂"查看文件"的多种姿势和"管道/重定向"这两个 Linux 最核心的抽象。
**对应教科书章节**：命令组 4 + 组合用法章。

| 编号 | 题型 | 考点 | 题目指令（只给目标） |
|---|---|---|---|
| 2-1 | 动手 | 创建与重定向 | 用 `cat` 配合 `>` 自己创建一个小文件；再分别用 `>` 和 `>>` 往里写内容两次，对比文件内容变化 |
| 2-2 ★ | 观察 | 覆盖 vs 追加 | 说明 `>` 与 `>>` 的区别，并设计一个小实验验证"覆盖"会先清空再写 |
| 2-3 | 动手 | 头尾查看 | 用 `head`/`tail` 看系统日志文件（如 /var/log/syslog）的头尾各 10 行；再用 `tail -f` 跟踪一个持续写入的文件，观察动态刷新 |
| 2-4 ★ | 动手 | 指定行数 | 从同一份日志中取出第 100 行到第 120 行的内容（head 与 tail 组合） |
| 2-5 | 动手 | grep 过滤 | 用 `grep` 从日志中找出含 error 的行；再用 `-i`/`-v`/`-c` 各做一遍，说明每个选项的意义 |
| 2-6 | 组合 | 递归搜索 | 用 `grep -r` 在整棵目录树里搜索一个关键词，并让它显示行号 |
| 2-7 | 组合 | 管道实战 | 把"查看 → 过滤 → 统计"串成一条管道：从日志里捞出含 error 的行，数出一共有多少条 |
| 2-8 ★ | 辨析 | 管道 vs 重定向 | 解释 `|` 和 `>` 的本质区别（数据流向命令 vs 流向文件），各举一个例子 |

**最终产出**：从一份真实日志中捞出指定信息并存入文件（附操作截图/记录），并用一句话向新手讲清"管道和重定向有什么不同"。

### 阶段 3｜权限与用户：chmod/chown/sudo/用户管理

**阶段目标**：真正搞懂 Linux 权限模型（属主/属组/其他人 × 读写执行），会建用户、建组、配置互不越权。
**对应教科书章节**：命令组 5~6。

| 编号 | 题型 | 考点 | 题目指令（只给目标） |
|---|---|---|---|
| 3-1 | 动手 | 权限位解读 | 用 `ls -l` 解读一个文件的权限串（如 -rw-r--r--），说出每位含义；用数字法算出 755、644 各代表什么权限 |
| 3-2 ★ | 动手 | 两种改法 | 对同一个文件，先用符号法（u+x）再用数字法（chmod 751）改权限，验证两种方法结果一致 |
| 3-3 | 动手 | 建用户 | 用 sudo 创建两个新用户 alice 和 bob，并设置密码 |
| 3-4 | 组合 | 越权验证 | 分别以 alice、bob 身份访问对方家目录，记录被拒绝的现象，并解释是哪一位权限拦住了 |
| 3-5 | 辨析 | chown 影响 | 把 alice 的一个文件 `chown` 给 bob 后，alice 还能不能读/改它？动手验证并解释 |
| 3-6 ★ | 动手 | 组共享 | 建一个共享组，把 alice、bob 都加进去，做一个"两人都能写"的共享目录，验证组权限的写法 |
| 3-7 | 观察 | 账号文件 | 查看 /etc/passwd 和 /etc/group，找到自己的账号行，解释每个冒号分隔字段的含义 |
| 3-8 ★ | 辨析 | 删除权限回顾 | 结合阶段 1 的 1-7：为什么在"目录可写但文件属主是别人"的情况下，你依然能删掉那个文件？用权限位推演一遍 |

**最终产出**：alice + bob + 一个共享组的完整体系：各自的私有目录互不越权、共享目录两人可写，并画一张权限判断流程图。

### 阶段 4｜进程与系统：ps/top/kill/服务/磁盘/包管理

**阶段目标**：理解"进程"概念和前后台机制，会定位和终止进程、管理服务、查磁盘、装软件。
**对应教科书章节**：命令组 7~9 + systemctl/apt 新章。

| 编号 | 题型 | 考点 | 题目指令（只给目标） |
|---|---|---|---|
| 4-1 | 动手 | 进程定位 | 启动一个后台进程（如 sleep 300 &），用 `ps` 找到它的 PID（进程号），说明 PID 是什么 |
| 4-2 ★ | 组合 | 快速定位 | 用 `ps aux | grep` 和 `pgrep` 两种方式定位同一个进程，对比两者差别 |
| 4-3 | 动手 | 优雅 vs 强杀 | 对"一个会写日志的进程"分别用 `kill` 和 `kill -9` 终止，对比日志里的差异，解释为什么优先用 `kill` |
| 4-4 ★ | 动手 | 资源观察 | 用 `top`/`htop` 找出当前最吃 CPU 和内存的进程，说出各列含义 |
| 4-5 | 动手 | 磁盘视角 | 用 `df -h` 看各分区占用、`du -sh` 看某目录大小，说清两者视角的区别 |
| 4-6 | 动手 | 服务管理 🌐 | 用 systemctl 对一个服务完成 status/start/stop/restart 全流程（无 systemd 的环境用 nohup 后台方式替代） |
| 4-7 | 动手 | 包管理 | 用 `apt` 搜索并安装一个工具（如 htop、curl），再用 `apt list` 确认装上了 |
| 4-8 ★ | 辨析 | 前后台机制 | 体验 Ctrl+Z 挂起、Ctrl+C 中断、jobs/fg/bg 切换，说清三种"停下来"方式的区别 |

**最终产出**：完整走一遍"起进程 → 定位 → 优雅终止"流程 + 一次磁盘占用调查记录 + 独立完成一次软件安装。

### 阶段 5｜查找与归档：find/grep -r/sort/uniq/tar

**阶段目标**：掌握 find 的查找语法、日志分析的"提取→排序→去重→统计"套路、tar 打包还原。
**对应教科书章节**：命令组 8~9 + 组合用法章。

| 编号 | 题型 | 考点 | 题目指令（只给目标） |
|---|---|---|---|
| 5-1 | 动手 | 按名查找 | 用 `find` 在整个家目录里找出所有 .log 结尾的文件 |
| 5-2 ★ | 动手 | 按属性查找 | 用 `find` 找出大于 100MB 的文件；再找出最近 1 天内修改过的文件 |
| 5-3 | 组合 | 查找+执行 | 用 find 对找到的文件批量执行操作（-exec 或 -delete），并说明为什么要小心使用 |
| 5-4 | 动手 | 排序去重 | 对一份有重复行的名单文件做 sort（排序）+ uniq（去重）+ uniq -c（计数）三步操作 |
| 5-5 ★ | 组合 | 日志统计套路 | 用一条管道完成"从日志提取关键词 → 排序 → 去重计数 → 按次数倒序取前 5"，这是日志分析的标准套路 |
| 5-6 | 动手 | 打包压缩 | 用 `tar` 把整个练习目录打包，再分别用普通打包和 gzip 压缩，对比两种产物大小 |
| 5-7 ★ | 动手 | 解包验证 | 把上一步的压缩包解到另一个目录，用 diff 对比验证"解出来的和原来完全一致" |
| 5-8 | 辨析 | 引号原理 | 解释 `find -name "*.log"` 里为什么通常要加引号（谁在展开通配符：shell 先展开还是 find 自己匹配） |

**最终产出**：一次完整的"全盘查找大文件 → 统计日志高频词 → 打包备份 → 还原验证"全流程记录。

---

## 五、进阶篇大纲（5 场景，预计 10~14 小时）

> 每题开头是"工单式背景"，**只给目标，不给过程**；过程与答案全部在书末对应答案页。
> 每个场景先按"场景搭建手册"复刻故障现场，再做题。

### 场景 1｜本地推理服务起不来（日志排查）

**背景工单**：你是某公司 AI 组的新运维。同事说他昨天在服务器上装了一个推理服务，今天早上启动失败，让你去处理。**机器上有个服务脚本 labservice.sh，它在某个目录下写日志。**

**搭建手册**：运行练习册提供的假服务脚本（模拟推理服务），按手册故意改坏一处配置，启动即失败，日志里会留下 1 行真正的根因。

| 编号 | 目标（只给这些） |
|---|---|
| 进1-1 | 确认服务当前状态：进程在不在、端口有没有在监听 |
| 进1-2 | 找到日志文件的位置，从中定位出"根本原因那一行"，并说明你是怎么排除其他干扰行的 |
| 进1-3 | 修复问题，重新启动服务，让服务成功跑起来 |
| 进1-4 | 验证服务可用（端口通了、日志显示正常），写一份 3 行的处理记录（做了什么、为什么） |

**最终产出**：一份故障处理记录 + 复述"日志四步法"的完整流程。

### 场景 2｜磁盘满应急（模型文件占满磁盘）🌐

**背景工单**：你负责的 GPU 服务器磁盘使用率报警到 92%，同事说服务开始写日志报"空间不足"。**你的任务：定位是什么占满了磁盘、安全清理、恢复服务、给出预防措施。**

**搭建手册（核心示范，详见第三节与示范题 3）**：用 `dd` 创建 200MB 镜像 → `mkfs.ext4` 格式化 → 挂载成独立分区 → 在分区里塞入"模型文件""旧日志""临时缓存"等文件直到接近写满 → 开始做题。做完卸载即可，真系统无任何影响。

| 编号 | 目标（只给这些） |
|---|---|
| 进2-1 | 用 `df -h` 确认是哪个挂载点满了，占用了百分之多少 |
| 进2-2 | 进入该分区，逐层用 `du` 定位"最大的目录"和"最大的文件"分别是什么 |
| 进2-3 | 判断哪些文件可以安全删除、哪些不能动（说明你的判断依据），完成清理 |
| 进2-4 | 验证磁盘恢复 + 写出 2 条预防措施（比如日志轮转、缓存清理计划） |

**最终产出**：一份"磁盘应急处理记录"（含定位过程、清理清单、预防措施）。

### 场景 3｜服务连不上（网络链路排查）

**背景工单**：同事反馈"我们部署在本地 8080 端口的服务突然连不上了"，让你按链路排查：**主机通不通 → 端口通不通 → 服务进程在不在 → 日志怎么说。**

**搭建手册**：在本机用假服务脚本起一个服务，故障由读者自己注入三选一：A 服务压根没起；B 服务起了但端口被别的进程占了；C 服务正常但监听了错误的地址。做完恢复现场。

| 编号 | 目标（只给这些） |
|---|---|
| 进3-1 | 用 `ping` 确认主机可达性（含 `ping localhost` 与 ping 网关的区别） |
| 进3-2 | 用 `ss -tlnp` 查看监听端口，判断目标端口有没有在听、被谁听 |
| 进3-3 | 用 `nc -zv` 或 `curl` 实测端口通不通，把"进程在听"和"真的能连上"分开判断 |
| 进3-4 | 定位故障点，修复，并复述"主机 → 端口 → 进程 → 日志"四步排查顺序 |

**最终产出**：一份网络故障排查记录 + 画一张排查顺序流程图。

### 场景 4｜服务与进程运维（含 GPU 进程定位）

**背景工单**：你在管一台"推理服务机器"，今天要完成：部署一个服务并让它开机自启、监控它、处理一个"卡死进程"，最后查清楚"显卡/文件被哪个进程占着"。

**搭建手册**：用假服务脚本分别以 systemd（🌐）或 nohup 后台方式部署；再故意制造一个死循环进程当作"卡死进程"。GPU 题分轨：有 NVIDIA 显卡的做真实题；无 GPU 做等价替代题（见第三节）。

| 编号 | 目标（只给这些） |
|---|---|
| 进4-1 | 部署服务：后台运行 + 确认进程和端口都正常 |
| 进4-2 | 用 top/ps 找出那个死循环进程，优雅终止它（并说明为什么不直接 -9） |
| 进4-3 | GPU 题：用 `nvidia-smi` 查看显卡状态，用 `fuser -v` 定位占用显卡的进程并处理。/ 无 GPU 替代题：定位"占用某个文件"的进程并处理（技能完全等价） |
| 进4-4 | 设置服务开机自启（systemd 🌐 / 或 rc.local / 或说明其他方式），重启验证自启生效 |

**最终产出**：一台"服务能自启、能监控、出问题会处理"的练习机状态 + 处理记录。

### 场景 5｜脚本收尾（衔接学习计划的 5 个脚本任务）

**背景工单**：前四个场景你都是手动敲命令处理的。现在公司要求把巡检"脚本化"：以后一键就能看到机器状态。**这就是通往学习计划里"5 个 Shell 脚本"的桥。**

| 编号 | 目标（只给这些） |
|---|---|
| 进5-1 | 写一个"资源巡检"脚本：一键输出磁盘占用（df）、内存（free）、CPU 负载（uptime）摘要 |
| 进5-2 | 写一个"服务健康检查"脚本：检查进程在不在 + 端口通不通 + 日志最近有没有更新，结果用中文提示输出 |
| 进5-3 | 给两个脚本加上变量、判断和注释，让一个完全没写过脚本的人能看懂 |
| 进5-4 | 运行验证两个脚本的输出正确，并（可选 🌐）用 cron 设置每天自动巡检一次 |

**最终产出**：2 个可用的 Shell 脚本，直接衔接学习计划第 1 层"5 个脚本"任务。

---

## 六、示范题（锁定质量标准，老师照此批量生产）

### 示范题 1｜基础篇 3-1 变式（动手题）

**题目**：你的共享目录里有个文件权限是 `-rw-r--r--`。请用**数字法**把它改成"属主可读可写可执行、属组可读可执行、其他人只读"，再用 `ls -l` 验证。

**参考答案（四步式）**：

1. **先想什么**：权限三位分别是 属主/属组/其他人，每位 = 读(4)+写(2)+执行(1) 求和。目标权限：属主 rwx = 4+2+1 = 7；属组 r-x = 4+0+1 = 5；其他人 r-- = 4。所以数字是 754。
2. **为什么用这个命令**：`chmod` 是唯一改权限的命令；数字法适合"一次性改完三位"，符号法适合"只动一位"，这里三位都要改，用数字法一步到位。
3. **完整步骤**：
   ```bash
   cd 共享目录
   ls -l 文件名        # 先看当前权限
   chmod 754 文件名
   ls -l 文件名        # 应显示 -rwxr-xr--
   ```
4. **怎么验证做对了**：`ls -l` 第一列变成 `-rwxr-xr--` 即正确；若显示 `-rwxr-xr-x` 说明最后一位算错（把其他人的写权限也加进去了）。

### 示范题 2｜基础篇 2-7（组合题）

**题目**：从 /var/log/syslog 里找出所有含 "error"（不区分大小写）的行，统计一共有多少条，并把结果存到 ~/lab/error_count.txt。

**参考答案（四步式）**：

1. **先想什么**：三个动作——找行（grep）、数数（wc -l）、存文件（重定向）。grep 的结果要交给 wc 数，这是"命令到命令"的数据流，应该用管道 `|`；wc 的结果要保存，这是"命令到文件"，用 `>`。
2. **为什么用这个命令**：`grep -i` 不区分大小写（能同时抓到 Error/ERROR）；`wc -l` 数行数；管道连接两者。不能用 `>` 把 grep 结果直接给 wc——重定向是给文件的，不是给命令的。
3. **完整步骤**：
   ```bash
   grep -i error /var/log/syslog | wc -l > ~/lab/error_count.txt
   cat ~/lab/error_count.txt   # 查看结果数字
   ```
4. **怎么验证做对了**：先不加重定向跑 `grep -i error /var/log/syslog | wc -l`，终端显示的数和文件里的数一致；再用 `grep -ic error /var/log/syslog` 交叉验证（-c 直接计数），两个数应该相同。

### 示范题 3｜进阶篇 场景 2 全流程（含完整搭建手册）

**背景工单**：磁盘报警 92%，服务写日志报"空间不足"。请定位、清理、恢复、给预防措施。

**搭建手册（做题前执行，需电脑 Ubuntu 🌐）**：

```bash
# ① 创建一个 200MB 的空白镜像文件（200 是大小，单位 MB）
dd if=/dev/zero of=~/lab/disk.img bs=1M count=200

# ② 格式化成 ext4 文件系统（在镜像里格式化，真磁盘不受影响）
mkfs.ext4 ~/lab/disk.img

# ③ 挂载成独立分区（需要 sudo；loop 让文件伪装成磁盘）
sudo mkdir -p /mnt/lab
sudo mount -o loop ~/lab/disk.img /mnt/lab

# ④ 塞入"故障文件"：模型文件、旧日志、缓存各一堆，直到接近写满
sudo mkdir -p /mnt/lab/models /mnt/lab/logs /mnt/lab/cache
sudo fallocate -l 60M /mnt/lab/models/model_v1.bin
sudo fallocate -l 60M /mnt/lab/models/model_v2.bin
sudo fallocate -l 30M /mnt/lab/logs/old_service.log
sudo fallocate -l 30M /mnt/lab/cache/tmp_cache.bin
df -h /mnt/lab        # 确认已接近写满，现场搭建完成
```

> ⚠️ 安全提示：故障全部发生在 `/mnt/lab` 这个镜像分区里。做完题执行 `sudo umount /mnt/lab` 卸载后，一切恢复原样，真系统没有任何变化。

**题目（只给目标）**：
- 进2-1：确认是哪个挂载点满了，占用多少。
- 进2-2：进入该分区，逐层定位最大的目录、最大的文件。
- 进2-3：判断哪些可安全删除、哪些不能动（说清依据），完成清理。
- 进2-4：验证恢复，写出 2 条预防措施。

**参考答案（四步式）**：

1. **先想什么**：磁盘满分两层看——"全局"（哪个分区满）用 `df -h`，"局部"（谁占的）用 `du` 一层层下钻，最后用 `ls -lhS` 按大小排序找元凶。清理前必须判断文件性质：`model_v1.bin` 是被 v2 替代的旧模型（可删），`old_service.log` 是旧日志（可删），`tmp_cache.bin` 是缓存（可删）；但如果服务正在写某个日志，那个就不能删。
2. **为什么用这个命令**：`df -h` 看分区级占用；`du -h --max-depth=1` 只下钻一层、不淹没在细节里；`ls -lhS` 用 S 参数按大小排序，一眼看到最大的文件；`rm` 删除。不用 `find -size` 是因为目录不大，逐层看更直观、更能练"下钻"的思路。
3. **完整步骤**：
   ```bash
   df -h /mnt/lab                                   # 进2-1：看到该分区使用率 90%+
   cd /mnt/lab
   du -h --max-depth=1 .                            # 进2-2：找出 models/ 最大
   du -h --max-depth=1 ./models                     # 下钻：model_v1/v2 各 60M
   ls -lhS ./models ./logs ./cache                  # 按大小排序确认元凶
   rm ./models/model_v1.bin                         # 进2-3：删被替代的旧模型
   rm ./logs/old_service.log ./cache/tmp_cache.bin  # 删旧日志和缓存
   df -h /mnt/lab                                   # 进2-4：使用率显著下降
   ```
4. **怎么验证做对了**：清理前后各跑一次 `df -h /mnt/lab`，使用率从 90%+ 降到明显更低的水平；`du -h --max-depth=1 .` 里 models/logs/cache 的大小对应减少。**收尾**：`sudo umount /mnt/lab` 卸载实验分区（安全红线，必须做）。

---

## 七、书的结构（给老师排版用）

1. 前言：定位、学习顺序、环境要求、题目语言规则、安全红线
2. 基础篇：5 阶段题目（只给目标）
3. 进阶篇：5 场景（背景工单 + 搭建手册 + 目标）
4. 参考答案：集中在书末，按阶段分节，每题四步式
5. 附录：命令速查表（配合教科书使用）、术语表
